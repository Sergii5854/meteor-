import { Meteor } from 'meteor/meteor';
import { Mongo } from 'meteor/mongo';

import Vocabruary from '../imports/api/collections';

import '../imports/startup/server'

Meteor.startup(() => {
  // code to run on server at startup
});



